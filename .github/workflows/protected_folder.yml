name: Protected folder guards

on:
  pull_request:
    branches: [ main ]
    paths:
    - 'internal/server/**'

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
    - name: Warning

      run: |
        echo "::warning:: This diff contains changes in protected folders!"
    
